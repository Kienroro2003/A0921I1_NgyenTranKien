let numbers = parseInt(prompt("Nhập số tự nhiên bất kì lớn hơn 0"));
while(numbers < 0){
    numbers = parseInt(prompt("Nhập lại số tự nhiên lớn hơn 0"));
}


let add;

for(let count = 0 ; count <= numbers ; count++){
    if (count >= 2){
        for(; count <= numbers - 1 ; count++){
            console.log(count);
            if (number - 1 % count !== 0){
                add += document.write
            }
        }
    }
}